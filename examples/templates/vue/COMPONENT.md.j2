# 组件实现：{{ desc }}

## 文件结构
```
src/components/{{ desc | slugify }}/
├── index.vue           # 组件主文件
├── index.test.ts       # 单元测试
├── index.stories.ts    # Storybook 故事
└── types.ts           # 类型定义
```

## 依赖项
（列出组件依赖的外部库）

## 使用示例
```vue
<template>
  <!-- 使用示例 -->
</template>
```
