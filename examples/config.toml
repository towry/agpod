# Example agpod configuration
# Place this at $XDG_CONFIG_HOME/agpod/config.toml (or ~/.config/agpod/config.toml)
# or .agpod.toml in your project root.

version = "1"

[log]
level = "warning"

[diff]
output_dir = "llm/diff"
large_file_changes_threshold = 100
large_file_lines_threshold = 500
max_consecutive_empty_lines = 2

[mcp.hive.claude.env_set]
# Shared environment variables applied to all hive Claude modes.
# PATH is inherited from the agpod-mcp parent process; add only explicit overrides here.
# ANTHROPIC_BASE_URL = "https://example.invalid"
# ANTHROPIC_AUTH_TOKEN = "token"

[mcp.hive.claude.modes.readonly]
description = "Read-only Claude worker for analysis and review."
command = "claude"
args = ["--dangerously-skip-permissions"]
# settings = "~/.claude/settings.json"
# mcp_config = "~/.claude/generated/mcp-readonly.json"
# system_prompt_file = "~/.config/agpod/prompts/readonly.md"
# system_prompt = "You are a read-only assistant."  # mutually exclusive with system_prompt_file
# env = { MAX_MCP_OUTPUT_TOKENS = "12000" }

[mcp.hive.claude.modes.full]
description = "Full-access Claude worker for implementation."
command = "claude"
args = []
# settings = "~/.claude/settings.json"
# mcp_config = "~/.mcp.json"
# system_prompt_file = "~/.config/agpod/prompts/full.md"
# env = {}
